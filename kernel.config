CONFIG_64BIT=y
CONFIG_PRINTK=y
CONFIG_PRINTK_TIME=y
CONFIG_TTY=y
CONFIG_SERIAL_8250=y
CONFIG_SERIAL_8250_CONSOLE=y
CONFIG_BINFMT_ELF=y
CONFIG_BINFMT_SCRIPT=y
CONFIG_BLK_DEV_INITRD=y
CONFIG_RD_GZIP=y
CONFIG_DEVTMPFS=y
CONFIG_DEVTMPFS_MOUNT=y
CONFIG_PROC_FS=y
CONFIG_SYSFS=y
CONFIG_TMPFS=y
CONFIG_MULTIUSER=y
CONFIG_FUTEX=y
CONFIG_EPOLL=y
CONFIG_SIGNALFD=y
CONFIG_TIMERFD=y
CONFIG_EVENTFD=y
CONFIG_SHMEM=y
CONFIG_PCI=y
CONFIG_VIRTIO=y
CONFIG_VIRTIO_PCI=y
CONFIG_VIRTIO_BLK=y
CONFIG_SQUASHFS=y
CONFIG_SQUASHFS_ZLIB=y
CONFIG_MD=y
CONFIG_BLK_DEV_DM=y
CONFIG_DM_VERITY=y
CONFIG_DM_INIT=y
CONFIG_CRYPTO_SHA256=y
CONFIG_CRYPTO_LIB_SHA256=y
CONFIG_BLK_DEV=y
CONFIG_BLOCK=y
CONFIG_MISC_FILESYSTEMS=y
CONFIG_CRYPTO=y
CONFIG_VIRTIO_MENU=y
CONFIG_DM_BUFIO=y
CONFIG_ZLIB_INFLATE=y
CONFIG_EFI=y
CONFIG_EFI_STUB=y
CONFIG_RELOCATABLE=y
CONFIG_ACPI=y
CONFIG_NET=y
CONFIG_UNIX=y
CONFIG_INET=y
CONFIG_PACKET=y
CONFIG_NETDEVICES=y
CONFIG_NET_CORE=y
CONFIG_ETHERNET=y
CONFIG_VIRTIO_NET=y
CONFIG_NET_VENDOR_INTEL=y
CONFIG_E1000E=y
CONFIG_NET_VENDOR_REALTEK=y
CONFIG_R8169=y

# --- usb uplink: android tether + usb-ethernet dongle for wifi-only laptops -
# firmware-free every one (verified in-tree). the wifi answer that keeps the
# firmware-free thesis: phone usb-tether or a usb-ethernet dongle.
#   USB_USBNET          minidriver framework (selects MII)
#   USB_NET_CDCETHER    cdc-ecm: newer android tether, many dongles
#   USB_NET_CDC_NCM     cdc-ncm: newest android/usb tether
#   USB_NET_RNDIS_HOST  rndis: most android usb tethering (accepted host-parser surface)
#   USB_NET_AX88179_178A  asix ax88179/178a gigabit dongles
#   USB_NET_AX8817X     asix ax88772 legacy dongles
#   USB_RTL8152         realtek rtl8152/8153 dongles (fw optional, links without it)
CONFIG_USB_USBNET=y
CONFIG_USB_NET_CDCETHER=y
CONFIG_USB_NET_CDC_NCM=y
CONFIG_USB_NET_RNDIS_HOST=y
CONFIG_USB_NET_AX88179_178A=y
CONFIG_USB_NET_AX8817X=y
CONFIG_USB_RTL8152=y

# --- real hardware: usb-stick boot ---------------------------------------
# the stick enumerates on xHCI (any secure-boot-capable machine is xHCI-era);
# EHCI/OHCI/UHCI are deliberately absent -- pre-xHCI boxes are out of scope.
CONFIG_USB_SUPPORT=y
CONFIG_USB=y
CONFIG_USB_PCI=y
CONFIG_USB_XHCI_HCD=y
CONFIG_USB_XHCI_PCI=y
CONFIG_SCSI=y
CONFIG_BLK_DEV_SD=y
CONFIG_USB_STORAGE=y
CONFIG_USB_UAS=y
# NO NVMe, NO AHCI/SATA on purpose: a kernel that cannot see the internal disk
# cannot touch it. "your disks are untouched" is enforced by absent drivers.

# --- real hardware: keyboard + visible console ---------------------------
CONFIG_INPUT=y
CONFIG_INPUT_KEYBOARD=y
CONFIG_SERIO=y
CONFIG_SERIO_I8042=y
CONFIG_KEYBOARD_ATKBD=y
CONFIG_HID_SUPPORT=y
CONFIG_HID=y
CONFIG_HID_GENERIC=y
CONFIG_USB_HID=y
CONFIG_VT=y
CONFIG_VT_CONSOLE=y
CONFIG_FB=y
# efifb, not simpledrm: simpledrm drags in the whole DRM core (~1 MiB). efifb
# claims the efi-framebuffer platform device sysfb registers. do NOT set
# SYSFB_SIMPLEFB -- it would hand that device to simplefb and efifb gets none.
CONFIG_FB_EFI=y
CONFIG_FRAMEBUFFER_CONSOLE=y
CONFIG_FONT_8x16=y

# --- kernel self-protection ----------------------------------------------
# tinyconfig leaves ALL of these off. the worst offender: CPU_MITIGATIONS=n
# compiled out every spectre/meltdown mitigation. these turn them on and are
# gated (G14) so a config regression fails the build, not just weakens it.
CONFIG_STACKPROTECTOR=y
CONFIG_STACKPROTECTOR_STRONG=y
CONFIG_CPU_MITIGATIONS=y
CONFIG_MITIGATION_PAGE_TABLE_ISOLATION=y
CONFIG_MITIGATION_RETPOLINE=y
CONFIG_MITIGATION_RETHUNK=y
# "the full mitigation set" was four symbols pinned and a dozen defaulting on
# under CPU_MITIGATIONS. defaults are not gates: pin every one that exists in
# this series, so a future default flip is a build failure, not a surprise.
CONFIG_MITIGATION_SPECTRE_V1=y
CONFIG_MITIGATION_SPECTRE_V2=y
CONFIG_MITIGATION_SPECTRE_BHI=y
CONFIG_MITIGATION_SRSO=y
CONFIG_MITIGATION_RFDS=y
CONFIG_MITIGATION_MDS=y
CONFIG_MITIGATION_TAA=y
CONFIG_MITIGATION_MMIO_STALE_DATA=y
CONFIG_MITIGATION_L1TF=y
CONFIG_MITIGATION_RETBLEED=y
CONFIG_MITIGATION_SRBDS=y
CONFIG_MITIGATION_SSB=y
CONFIG_MITIGATION_SLS=y
CONFIG_MITIGATION_IBPB_ENTRY=y
CONFIG_MITIGATION_IBRS_ENTRY=y
CONFIG_MITIGATION_UNRET_ENTRY=y
CONFIG_MITIGATION_CALL_DEPTH_TRACKING=y
CONFIG_MITIGATION_GDS=y
CONFIG_MITIGATION_ITS=y
CONFIG_MITIGATION_TSA=y
# not MITIGATION_VMSCAPE: it guards a hypervisor against its guests and
# depends on KVM, which this kernel does not have.
# dmesg restricted from the first printk, not from init's sysctl pass -- the
# window between them was the one moment the ring buffer was world-readable.
CONFIG_SECURITY_DMESG_RESTRICT=y
CONFIG_HARDENED_USERCOPY=y
CONFIG_FORTIFY_SOURCE=y
CONFIG_INIT_ON_ALLOC_DEFAULT_ON=y
# alloc-side zeroing alone leaves freed pages holding secrets until realloc;
# free-side zeroing closes that (use-after-free reads zeros, cold-boot residue
# shrinks). the few-percent alloc cost is nothing on a shell workload.
CONFIG_INIT_ON_FREE_DEFAULT_ON=y
CONFIG_BUG_ON_DATA_CORRUPTION=y
CONFIG_ZERO_CALL_USED_REGS=y
CONFIG_RANDOMIZE_KSTACK_OFFSET=y
CONFIG_RANDOMIZE_KSTACK_OFFSET_DEFAULT=y
CONFIG_SLAB_FREELIST_RANDOM=y
CONFIG_SLAB_FREELIST_HARDENED=y
CONFIG_RANDOM_KMALLOC_CACHES=y
CONFIG_SHUFFLE_PAGE_ALLOCATOR=y
CONFIG_X86_UMIP=y
CONFIG_PANIC_ON_OOPS=y
# already on via tinyconfig -- pinned so a future defconfig drift fails loud
CONFIG_RANDOMIZE_BASE=y
CONFIG_RANDOMIZE_MEMORY=y
CONFIG_VMAP_STACK=y
CONFIG_STRICT_KERNEL_RWX=y

# --- lockdown LSM (compiled-in confidentiality, > any cmdline knob) -------
CONFIG_SECURITY=y
CONFIG_SECURITYFS=y
CONFIG_SECURITY_LOCKDOWN_LSM=y
CONFIG_SECURITY_LOCKDOWN_LSM_EARLY=y
CONFIG_LOCK_DOWN_KERNEL_FORCE_CONFIDENTIALITY=y

# --- assert-OFF: attack surface that must never come back -----------------
# the build fails if any of these is =y. most are already off under tinyconfig;
# pinning them makes a future config change that re-enables them a build error.
CONFIG_MODULES=n
CONFIG_SLUB_TINY=n
CONFIG_SLAB_MERGE_DEFAULT=n
CONFIG_X86_VSYSCALL_EMULATION=n
CONFIG_LEGACY_TIOCSTI=n
CONFIG_X86_IOPL_IOPERM=n
CONFIG_X86_MPPARSE=n
CONFIG_LOCK_DOWN_KERNEL_FORCE_NONE=n
CONFIG_DEVMEM=n
CONFIG_DEVPORT=n
CONFIG_IO_URING=n
CONFIG_BPF_SYSCALL=n
CONFIG_DEBUG_FS=n
CONFIG_PROC_KCORE=n
CONFIG_PROC_VMCORE=n
CONFIG_KEXEC=n
CONFIG_KEXEC_FILE=n
CONFIG_HIBERNATION=n
CONFIG_IA32_EMULATION=n
CONFIG_USERFAULTFD=n
CONFIG_MODIFY_LDT_SYSCALL=n
CONFIG_COREDUMP=n
CONFIG_E1000=n
# the product thesis -- "xos ships no driver that can see your disks" -- was a
# comment above the usb block, so a driver that `select`s ATA on some future
# olddefconfig pass would have shipped green. now it is a contract: no sata,
# no nvme, no sd/mmc, no raw flash. usb-storage over xhci is the only block path.
CONFIG_ATA=n
CONFIG_SATA_AHCI=n
CONFIG_BLK_DEV_NVME=n
CONFIG_MMC=n
CONFIG_MTD=n
# namespaces are a classic root-from-unprivileged path and this box runs
# everything as root anyway; binfmt_misc registers new interpreters at runtime.
CONFIG_NAMESPACES=n
CONFIG_USER_NS=n
CONFIG_BINFMT_MISC=n

# ptys. these were already on -- but only because olddefconfig put them there,
# which means G14 was not defending them and a config change could have dropped
# them silently. every multiplexer and every ssh session needs a pty, so pin the
# one we use and turn off the obsolete interface we do not.
CONFIG_UNIX98_PTYS=y
CONFIG_LEGACY_PTYS=n

# ── writable filesystems ────────────────────────────────────────────────────
# until now the only filesystem driver was squashfs, which meant xos could not
# mount a usb stick someone handed it -- only read the raw bytes with dd. that
# is a real limit for a tool whose job is showing up on unfamiliar machines.
#
# this does NOT weaken the host-disk guarantee. that property comes from having
# no NVMe and no AHCI driver, so the host's internal disks never enumerate at
# all. adding filesystem drivers changes nothing about what block devices exist.
# file locking. this was compiled out, which meant flock() returned ENOSYS --
# so cryptsetup could not take its device locks, AND the flock applet that this
# system ships and the curriculum teaches had never worked at all. the gates
# could not see it: answers are graded by running them on the build host, where
# flock works. a shipped command that is broken only on the target is exactly
# what the boot-time probes are for.
CONFIG_FILE_LOCKING=y

# loop devices. losetup has been a shipped applet this whole time with no
# driver behind it -- it could not work at all. it is also how you examine a
# disk image as a block device without writing it to a disk first.
CONFIG_BLK_DEV_LOOP=y
CONFIG_EXT4_FS=y
CONFIG_FAT_FS=y
CONFIG_VFAT_FS=y
# exFAT: big SD cards and modern usb sticks (anything over 32 GB is usually
# this, not vfat). ISO9660: mount a .iso as a loop device. NTFS3: read a
# windows laptop's disk -- but READ ONLY. every filesystem driver is a parser
# running in the kernel on bytes from a disk you do not trust, which is the
# exact "get hacked back" vector this stick exists to resist. read-only shrinks
# that surface, and the SAFEST read of a hostile disk is always dd at the block
# level, where no parser runs at all. NTFS3_FS_POSIX_ACL and the write path are
# left off deliberately.
CONFIG_EXFAT_FS=y
CONFIG_ISO9660_FS=y
CONFIG_NTFS3_FS=y
# NTFS3 read-write exists but is not enabled: writing a hostile or unfamiliar
# ntfs volume is how you corrupt someone's laptop or trip a parser bug on the
# write path. read is the capability that matters here.
# (mounting is forced ro,nosuid,nodev,noexec by init regardless.)
CONFIG_NLS_CODEPAGE_437=y
CONFIG_NLS_ISO8859_1=y
CONFIG_NLS_UTF8=y

# ── encrypted AND authenticated state (p3) ──────────────────────────────────
# dm-crypt alone gives confidentiality only: decrypting tampered ciphertext
# yields attacker-controlled garbage that the ext4 parser then consumes as root.
# dm-integrity makes tampering refused rather than parsed -- the same fail-closed
# property dm-verity gives the read-only root. the pair is the point; neither
# half is sufficient.
CONFIG_DM_CRYPT=y
CONFIG_DM_INTEGRITY=y
CONFIG_CRYPTO_AES=y
CONFIG_CRYPTO_XTS=y
CONFIG_CRYPTO_HMAC=y
CONFIG_CRYPTO_SHA512=y
CONFIG_CRYPTO_CBC=y

# the userland crypto backend, via AF_ALG. this is what lets cryptsetup use the
# kernel's own crypto instead of linking openssl or gcrypt -- one large pinned
# dependency avoided, on a repo that ships four binaries total.
CONFIG_CRYPTO_USER_API=y
CONFIG_CRYPTO_USER_API_HASH=y
CONFIG_CRYPTO_USER_API_SKCIPHER=y

# entropy is NOT configured here. CONFIG_RANDOM_TRUST_CPU was removed as a
# Kconfig symbol; the kernel controls it with the random.trust_cpu cmdline parameter,
# which defaults to true in the code. it is pinned explicitly on the cmdline
# instead (see build.sh verity + G15) so a kernel bump cannot flip a default
# that key generation depends on.

# ── wireguard: the tunnel xos dials OUT through ──────────────────────────────
# remote access works by xos connecting outward to a host you control, so xos
# never listens on the untrusted network it is plugged into. wireguard is in
# the kernel (its crypto -- curve25519, chacha20poly1305, blake2s -- comes with
# it), roams across a changing IP, and resumes a dropped link rather than
# resetting it. that roaming is one of the three things that make a dropped
# connection survivable (the others are dropbear-on-wg staying invisible, and
# abduco keeping the session alive).
CONFIG_WIREGUARD=y
# NOT enabling network namespaces just to test wireguard: NET_NS pulls in the
# whole NAMESPACES subsystem, which is real attack surface (namespace setup is a
# classic privilege-escalation path) for a machine that runs everything as root.
# the self-test proves the wireguard interface comes up, is keyed and configured,
# and that dropbear accepts ssh over it via loopback; the actual two-machine
# handshake is verified on real hardware, as the plan requires for anything past
# the qemu-testable phases.
