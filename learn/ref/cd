cd
Usage: cd [DIR]

Change the shell's working directory to DIR.

With no argument, go to $HOME. It must be a builtin: a separate
program could only change its own directory and then exit.

	cd /etc		absolute path
	cd ..		up one level
	cd		home
	cd -		back to the previous directory

See also: pwd, ls
