trap
Usage: trap [HANDLER SIGNAL...]

Run a command when a signal arrives.

Shell builtin. EXIT is a pseudo-signal that fires when the shell ends,
which is how scripts clean up reliably.

	trap 'rm -f "$tmp"' EXIT
	trap '' INT		ignore ctrl-c
	trap			list current traps

See also: kill, exit
