type
Usage: type NAME...

Show whether NAME is a builtin, function, alias, or program.

Shell builtin. Useful when a command does not behave the way the manual
for the program says -- often because it is a builtin and never reached
the program at all.

	type cd		cd is a shell builtin
	type ls		ls is /bin/ls

See also: which, hash
