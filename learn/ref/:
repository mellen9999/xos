:
Usage: :

Do nothing, successfully.

The null command: it expands its arguments and returns 0.

	while :; do ... done	loop forever
	: > FILE		truncate a file to nothing
	: ${VAR:=DEFAULT}	set VAR only if it was unset

See also: true, false
