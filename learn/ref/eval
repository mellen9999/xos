eval
Usage: eval ARG...

Join the arguments and run the result as a shell command.

A shell builtin, powerful and dangerous: anything in the string is
executed, so never eval data you did not construct yourself.

See also: exec, set
